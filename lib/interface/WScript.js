function WScript() {
}

/**
 * Output string to console
 * @param s String to output
 */
WScript.prototype.Echo = function(s) { };
/**
 * Make script sleep for a while
 * @param time Time to sleep in milliseconds
 */
WScript.prototype.Sleep = function(time) { };
